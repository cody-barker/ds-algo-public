function binarySearch(array, target) {
    //variables for start, mid, and end
    //check if mid === target, return target
    //if mid < target, start = mid + 1
    //if mid > target, end ==


    /*
    const array = [1,2,3,4,5,7,10]
    target = 7
    start = array[0]
    end = array
    */
}